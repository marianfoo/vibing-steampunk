# vsp Environment Configuration
# Copy this file to .env and fill in your SAP credentials.
#
# This is the DEFAULT system used when running vsp without --system flag.
# For multiple systems, use .vsp-systems.json instead.
#
# Priority: CLI flags > Environment variables > .env > Defaults

# SAP Connection (required)
SAP_URL=http://your-sap-host:50000
SAP_USER=YOUR_USERNAME
SAP_PASSWORD=YOUR_PASSWORD

# SAP Options (optional)
SAP_CLIENT=001
SAP_LANGUAGE=EN
SAP_INSECURE=false

# Tool Mode (optional)
# focused = 19 essential tools (default)
# expert = all 45+ tools
SAP_MODE=focused

# Safety Options (optional)
# SAP_READ_ONLY=false
# SAP_BLOCK_FREE_SQL=false
# SAP_ALLOWED_PACKAGES=Z*,Y*
# SAP_ALLOWED_OPS=RSQ

# Feature Flags (optional, values: auto/on/off)
# SAP_FEATURE_HANA=auto
# SAP_FEATURE_ABAPGIT=auto
# SAP_FEATURE_RAP=auto
# SAP_FEATURE_AMDP=auto
# SAP_FEATURE_UI5=auto
# SAP_FEATURE_TRANSPORT=auto

# ---------------------------------------------------------------------------
# Integration Test Connection (optional)
# ---------------------------------------------------------------------------
# Use TEST_SAP_* variables to point integration tests at a dedicated SAP system
# (or a different user) without overriding the SAP_* vars used by the running
# MCP server.  TEST_SAP_* takes priority over SAP_* for all integration tests.
#
# Run integration tests:
#   go test -tags=integration -v ./pkg/adt/
#
# If neither TEST_SAP_* nor SAP_* are set the integration tests are skipped
# automatically — no SAP system required for the unit test suite (go test ./...).
#
# TEST_SAP_URL=http://your-sap-host:50000
# TEST_SAP_USER=YOUR_TEST_USERNAME
# TEST_SAP_PASSWORD=YOUR_TEST_PASSWORD
# TEST_SAP_CLIENT=001
# TEST_SAP_LANGUAGE=EN
# TEST_SAP_INSECURE=false
